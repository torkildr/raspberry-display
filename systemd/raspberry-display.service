[Unit]
Description = Led display driver with MQTT client
After = network.target

[Service]
ExecStart = /usr/bin/raspberry-display-mqtt
Restart = always
User = PLACEHOLDER_USER
Nice = -10

[Install]
WantedBy = multi-user.target
